namespace Ai2dShooter.Common
{
    public enum Direction { North, East, South, West, Count }

    public enum PlayerController { Human, AiFsm, AiDt, Count }

    public enum Teams { TeamHot, TeamCold, Count }
}
